package authforward

// Fixture values shared by the tests in this package.
const (
	testToken     = "abc123"
	testBearer    = "Bearer " + testToken
	testBearerNew = "Bearer new"
	testBearerOld = "Bearer old"
)
